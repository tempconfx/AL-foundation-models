name: margins
params:
  enable_dropout: false  # whether to enable dropout for querying
  typical_features: false  # sample points with median scores
  cluster_features: false  # oversample and cluster candidates
  oversample: 50  # oversampling factor 